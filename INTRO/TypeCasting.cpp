#include <bits/stdc++.h>
using namespace std;

int main() {
    float f=99.99;
    int value=int(f);

    cout<<value; //this will give 99 bcz in the c++ its the ans before the decimal even its .99 
    return 0;
}

//this is type casting we do it by self convert the big data type to the smaller one.

//conversion ---- implicit
//casting ---- explicit